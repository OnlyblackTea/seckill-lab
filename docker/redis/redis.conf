# Redis 7 配置（Stage 5 缓存三件套 / Stage 6 分布式锁用）
# 持久化：AOF 打开，便于观察写入
appendonly yes
appendfsync everysec

# 内存与淘汰策略：缓存场景常用 allkeys-lru
maxmemory 256mb
maxmemory-policy allkeys-lru

# 基础
bind 0.0.0.0
protected-mode no
tcp-keepalive 60
loglevel notice
